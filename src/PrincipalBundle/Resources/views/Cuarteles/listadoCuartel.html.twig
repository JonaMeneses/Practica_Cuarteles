{% extends 'PrincipalBundle:Plantillas:PlantillaAdmin.html.twig' %}

{% block title %}
Cuerpo
{% endblock %}

{% block body %}

<div class="container">
  <div class="panel panel-default">
      <div class="panel-heading">

  <h2><span class="glyphicon glyphicon-list-alt"></span> &nbsp; Registro de Cuarteles</h2>
  <hr>
  <a href="{{ path('addCuartel') }}" class="btn btn-success">Nuevo Cuartel</a>
  <hr>
</div>
  <table class="table table-bordered">
    <thead>
      <th>Id</th>
      <th>Nombre</th>
      <th>Ubicacion</th>
      <th>Opciones</th>
    </thead>
    <tbody>
      {% for a in Cuarteles %}
        <tr>
          <td>{{a.id}}</td>
          <td>{{a.nombre}}</td>
          <td>{{a.ubicacion}}</td>
          <td><a href="{{path('delete_Cuartel',{id:a.id})}}" class="btn btn-sucess">
              <span class="glyphicon glyphicon-trash"></span>
            </a>
            <a href="{{path('edit_Cuartel',{id:a.id})}}" class="btn btn-info">
              <span class="glyphicon glyphicon-edit"></span>
            </a>
          </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
  </div>
</div>
  
{% endblock %}
{% block pie %}

{% endblock %}


